# Supabase 連線設定
# 這兩個值「設計上就是公開的」，會被打包進前端 bundle，任何人都能在瀏覽器看到，
# 不是機密。真正的存取控制靠 Supabase RLS（見 supabase/migrations/0001_phase1_schema.sql）。
#
# 使用方式：把本檔複製成 .env（同目錄），值已幫你填好本專案的值。
# 取得處：Supabase Dashboard → Project Settings → API
#   - Project URL          → VITE_SUPABASE_URL
#   - Project API keys 的 anon / public key → VITE_SUPABASE_ANON_KEY

VITE_SUPABASE_URL=https://bxdbacgakdrhbuqbbmmn.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_pM-W16CrDbjcbI2xDr9hOA_ku6dg4ok
